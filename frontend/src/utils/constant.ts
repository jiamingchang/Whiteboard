export const enum CodeDictionary {
  SUCCESS = 0,
}

export const enum USER_URLS {
  LOGIN = '/login',
  ADD = '/addUser',
  DELETE = '/deleteUser',
}

export const enum StorageKey {
  TOKEN = 'TOKEN',
  USER_INFO = 'USER_INFO',
}